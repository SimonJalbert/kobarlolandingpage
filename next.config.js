/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 15 no longer needs these options
  // experimental: {
  //   appDir: true,
  // }
};

module.exports = nextConfig; 